[package]
name = "frust-orm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true
publish.workspace = true
description = "Ported framework-orm-adapter: diff-based, idempotent, transactional, ordered, locked schema migrations — resource source swapped to runtime DocType metadata (WO-005 module 3)."

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[dev-dependencies]
# live-server tests (surreal.exe on :8899) replace the SDK's kv-mem harness
ureq = { workspace = true, features = ["json"] }
