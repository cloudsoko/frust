[package]
name = "{{app_name}}"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"

[dependencies]
wit-bindgen = "0.46"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
strip = true

[workspace]
