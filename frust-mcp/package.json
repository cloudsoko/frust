{
  "name": "frust-mcp",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "WO-059 probe: a Model Context Protocol server that is a CLIENT of the Frust REST surface. Generates MCP tools from GET /meta; tools/call forwards the caller's bearer token to /read /write /transition; all permission enforcement stays in the kernel (the fourth consumer of the one permission compiler).",
  "engines": { "node": ">=20" },
  "bin": { "frust-mcp": "src/server.mjs" },
  "scripts": {
    "start": "node src/server.mjs",
    "proof": "node proof/containment.mjs",
    "inspector": "pnpm dlx @modelcontextprotocol/inspector node src/server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  }
}
