---
tags: [frust, work-order, milestone-4, gate, assessment]
status: COMPLETE (2026-07-31) — **VERDICT: M4 DOES NOT CLOSE. One blocker, found by criterion 0's own restore.** **Criterion 0 GREEN on a dedicated fresh store** (a second SurrealDB over a scratch data dir; the live dev directory was never swapped): hook chain 0 ms/30, **submit warm median 4 ms/25**, realtime tax 0.05 ms/2 — **WO-050's unmeasured claim is retroactively earned**, 4 ms inside the historical fresh-store band (WO-040 4 · WO-041 3 · WO-044 2) at 6× headroom; stated precisely, integer-ms resolution cannot resolve a sub-ms loop cost, only that the floor did not move. **THEN THE RESTORE REFUSED TO BOOT — the blocker:** `E_BOOT_DB: refusing destructive change(s) ["REMOVE FIELD crm_followup"]`. **WO-050's extension uninstall can leave a store that will not start.** Traced: `detach_extensions` removes the field from METADATA (correct, WO-019's 'metadata detaches, data remains'); the COLUMN survives with its data (also correct, that IS the promise); the next boot's `MetadataSync` diffs record vs schema, sees an undeclared column, classifies it destructive; boot refuses (fail-closed, working); and **`BootOptions{holder, accept_meta_migrations}` has NO `allow_destructive`, so the refusal has no operator remedy** — the kernel will not start until someone hand-edits the DB. WO-019's own uninstall never hit this because deleting an app's OWN doctype removes the whole metadata row, so no diff exists; an extension uninstall leaves the row standing minus one field, which is exactly the case that produces one. **This is the honest-uninstall promise colliding with the destructive-change guard, neither wrong alone — resolution is an ADR-008-shaped RULING, deliberately not decided here** (assessment WO). Dev store remediated manually by re-declaring `crm_followup` with `orphaned_from:'crm'` (keeps column AND row data, diff empty) — boot green, workflow 18/18; **a remediation of one store, NOT a fix; the defect is untouched in code.** **Criterion 1 disposition:** 11 DONE (tenancy/P-8.1, conn-reuse, admission, re-skin, email, root-auth, hygiene, script-cache, print-interactive, extension/P-2.2, frontend-posture-ratified), **2 DEFERRED BY RULING and stated as rulings** (PDF engine → ADR-014 activates on the WO-043 attachment pull; hook-vocabulary → ADR-015 ordinary REQ-2.2.1 work), **1 NOT DONE** — Desk→kernel streaming, its not-a-blocker reasoning **RE-CHECKED not inherited** (WO-035 ~135 → WO-038 re-recorded ~146 req/s with 48 SSE subs coexisting, 0 failures; WO-032's relocates-not-eliminates finding stands) → confirmed optimization. **DB write ceiling stated honestly:** the old '~150 w/s raw' is superseded and should stop being quoted — WO-044 showed 124 req/s was argon2 not the DB, WO-048 showed removing the last root call moved JWT throughput not at all (334.8→338.9). Measured today fresh-store: **saturates c=2 at ~335 req/s**, flat throughput with p50 linear in concurrency (4.2→35.7 ms) = DB-bound with idle kernel workers. **No single authoritative number exists** — fresh-store readings on this machine span ~335–555 req/s; the shape is stable, the absolute is machine-state sensitive, and anyone quoting one figure should say which run. **Criterion 2:** tally = rows (24·10·0, PM correction recorded in the tally line itself); no bounded-by-assumption re-crept (the v2.0 gate's three closed in WO-035/036, P-2.2 since killed by building); P-2.2's kill honest-not-total and its row says so. **M5 seeded from the true remainder**, headed by the blocker's ruling+fix **and the missing regression that would have caught it: restart the kernel after an extension uninstall.** See [[2026-07-31 WO-051 milestone 4 close-out]].

Prior status — ACTIVE (2026-07-31) — assessment not build; the M4 capstone, WO-034 precedent (a gate that cannot fail is decorative)
created: 2026-07-31
---

# WO-051: Milestone 4 Close-Out

## Why

M4's declared basket ("turnkey & scalable") appears spent: tenancy, batteries, admission, re-skin, extension model — all closed. "Appears" is not a verdict. The WO-034 precedent governs: walk every seeded item's disposition honestly, let the gate be able to fail, and seed M5 from the true remainder.

## Criteria

0. **The outstanding WO-050 boundary item, FIRST: fresh-store perf gates (floor + tax).** WO-050 rewrote the write-path dispatch (single call → contributor loop, cache now carries `HookPlan`, keys widened) and claimed no number because "a loop of one is the same call" — **that is an inference, and this project's own history (the 640-arithmetic) says inferences about performance are measured, not accepted.** Run the gates on a dedicated fresh store. Green → WO-050's claim is retroactively earned. Red → a finding, reported before anything else in this WO proceeds.
1. **Disposition table — every M4 item, evidence-linked, honestly classified** (done / deferred-by-ruling / not-done): multi-DB tenancy + per-tenant restore (WO-039/040, P-8.1); email battery (WO-043); print interactive + engine-deferred (WO-045/046, ADR-014 — deferral is a RULING, state it as such); admission control (WO-038); the DB write ceiling's honest current status (WO-044 reframed the old ~150 w/s bound — what IS the measured ceiling story now, stated without inflation); UI re-skin + behavior layer (WO-042); cross-app extension (WO-049/050, P-2.2); root-auth + connection-reuse + hygiene arc (WO-041/044/047/048); **Desk→kernel streaming (NOT done — named, with its standing not-a-blocker reasoning re-checked, not inherited)**.
2. **Scorecard coherence:** tally = rows (24 · 10 · 0 as corrected); every surviving bounded verdict carries a measured number or a named architecture trade — the bounded-by-assumption class must not have re-crept since WO-036 closed it.
3. **Verdict: M4 COMPLETE or blockers named.** No middle. If complete, the milestone banner moves and **M5 is seeded from the honest remainder**: the SSR-native app-building kit (timing coupled to topcoat #275, per ADR-016); hook-vocabulary widening (REQ-2.2.1 build work, per ADR-015); REST-surface documentation + additive-only evolution policy (ADR-016's priced obligation, first installment); Desk→kernel streaming; the PDF engine (activates when the WO-043 attachment need is pulled, per ADR-014); watch items (6 upstream Topcoat PRs, #275).

## Boundaries

- Assessment, not build — the only code run is criterion 0's gates. Grade-inflation rule binds its author, as always.
- Anything found mis-dispositioned is a finding for the table, not a quiet fix.
